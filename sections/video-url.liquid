{% comment %}
** Video - homepage partial **
- Draggable section
{% endcomment %}
{% if section.settings.video contains 'youtu' %}
  {% if metafields contains 'youtu.' %}
    {% assign video_id = metafields | split: '/' | last %}
  {% elsif metafields contains 'youtube' %}
      {% assign video_id = metafields | split: '=' | last %}
  {% endif %}

    <div class="container main content video-section margin-bottom-50">
      <iframe width="420" height="315"
        src="https://www.youtube.com/embed/{{ video_id }}?autoplay=1&mute=1">
        </iframe>
    </div>
{% endif %}


{% schema %}
{
  "name": "Video by URL",
  "class": "video-section under-menu",
  "settings": [
    {
      "type": "url",
      "id": "video",
      "label": "Video"
    }
  ],
  "presets": [{
    "name": "Video by URL",
    "category": "Video by URL"
  }]
}

{% endschema %}
